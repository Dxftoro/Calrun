import "ref.dart";

class Settings {
  static final taskCount = Ref(20);
  static final tpaPrecision = Ref(2);
	static final minMinutes = Ref(0);
	static final maxMinutes = Ref(10);
	static final minPlusSeconds = Ref(5);
	static final maxPlusSeconds = Ref(30);
}